const annoCorrente = 2024; 
const annoNascita = 1984; 

const età = annoCorrente - annoNascita;
console.log(età);

const anniRaggiungimentoCento = 100 - età;
console.log(`La persona necessita di ${anniRaggiungimentoCento} anni per arrivare a 100`);